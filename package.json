{
  "name": "governance-ai-agents",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "description": "Laboratorio de aprendizaje: capa de gobernanza para agentes de IA, fase por fase y 100% TypeScript.",
  "scripts": {
    "demo": "tsx src/demo/run-demo.ts",
    "test": "pnpm test:phase1",
    "test:baseline": "vitest run src/tests/tools.test.ts src/tests/agent-loop.test.ts",
    "test:phase1": "vitest run src/tests/policy.test.ts src/tests/agent-gate.test.ts",
    "test:phase0": "pnpm test:baseline",
    "test:phase2": "vitest run --config vitest.future.config.ts src/tests/future/identity.test.ts src/tests/future/trust.test.ts",
    "test:phase3": "vitest run --config vitest.future.config.ts src/tests/future/guardrails.test.ts",
    "test:phase4": "vitest run --config vitest.future.config.ts src/tests/future/audit.test.ts",
    "test:phase5": "vitest run --config vitest.future.config.ts src/tests/future/limits.test.ts src/tests/future/approvals.test.ts",
    "test:phase6": "vitest run --config vitest.future.config.ts src/tests/future/governance-gate.test.ts",
    "test:all": "vitest run --config vitest.all.config.ts",
    "test:watch": "vitest",
    "typecheck": "pnpm typecheck:phase1",
    "typecheck:baseline": "tsc --noEmit -p tsconfig.baseline.json",
    "typecheck:phase0": "pnpm typecheck:baseline",
    "typecheck:phase1": "tsc --noEmit -p tsconfig.phase1.json",
    "typecheck:phase2": "tsc --noEmit -p tsconfig.phase2.json",
    "typecheck:phase3": "tsc --noEmit -p tsconfig.phase3.json",
    "typecheck:phase4": "tsc --noEmit -p tsconfig.phase4.json",
    "typecheck:phase5": "tsc --noEmit -p tsconfig.phase5.json",
    "typecheck:phase6": "tsc --noEmit -p tsconfig.phase6.json",
    "typecheck:all": "tsc --noEmit -p tsconfig.all.json",
    "verify": "pnpm verify:phase1",
    "verify:phase0": "pnpm test:phase0 && pnpm typecheck:phase0 && pnpm demo",
    "verify:phase1": "pnpm test:phase1 && pnpm typecheck:phase1",
    "verify:phase2": "pnpm test:phase1 && pnpm test:phase2 && pnpm typecheck:phase2",
    "verify:phase3": "pnpm test:phase1 && pnpm test:phase2 && pnpm test:phase3 && pnpm typecheck:phase3",
    "verify:phase4": "pnpm test:phase1 && pnpm test:phase2 && pnpm test:phase3 && pnpm test:phase4 && pnpm typecheck:phase4",
    "verify:phase5": "pnpm test:phase1 && pnpm test:phase2 && pnpm test:phase3 && pnpm test:phase4 && pnpm test:phase5 && pnpm typecheck:phase5",
    "verify:phase6": "pnpm test:phase1 && pnpm test:phase2 && pnpm test:phase3 && pnpm test:phase4 && pnpm test:phase5 && pnpm test:phase6 && pnpm typecheck:phase6",
    "verify:all": "pnpm test:all && pnpm typecheck:all",
    "verify:baseline": "pnpm test:baseline && pnpm typecheck:baseline && pnpm demo"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
